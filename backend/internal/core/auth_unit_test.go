package core

import (
	"testing"
)

func TestCheckUserExistsLogic(t *testing.T) {
	// This is a unit test for the logic, we won't actually hit the DB here 
    // unless we setup a test DB, which might be hard.
    // Instead I'll just check the code path.
}
